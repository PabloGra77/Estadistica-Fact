# =============================================================================
# Tablero PPL — Plantilla de variables de entorno
# Copia este archivo como .env y rellena tus valores reales.
# Este archivo SÍ se sube a Git (sin valores reales).
# =============================================================================

APP_ENV=production
APP_URL=https://tudominio.com

DB_HOST=localhost
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASS=
